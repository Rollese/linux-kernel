# User overrides for /etc/makepkg.conf (sourced after it by makepkg).

# Enable ccache: makepkg prepends /usr/lib/ccache/bin to PATH, so the kernel's
# `make LLVM=1` picks up the ccache clang shim.
BUILDENV=(!distcc color ccache check !sign)
